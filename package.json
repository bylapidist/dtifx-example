{
  "name": "dtifx-example",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal production-ready DTIFx stack example",
  "engines": {
    "node": "^24.0.0",
    "npm": ">=10"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --check",
    "format:write": "prettier . --write",
    "design-lint": "design-lint lint \"src/**/*.{css,js,jsx,ts,tsx}\" --config design-lint.config.cjs",
    "dtif:validate": "dtifx build validate --config build/dtif-build.config.mjs",
    "dtif:build": "dtifx build generate --config build/dtif-build.config.mjs",
    "dtif:diff": "dtifx diff compare ops/artifacts/diff/baseline.dtif.json tokens/catalog.json --format json --output ops/artifacts/diff/report.json && node -e \"const fs=require('node:fs');const path=require('node:path');const report='ops/artifacts/diff/report.json';const from='file://'+process.cwd().split(path.sep).join('/');const to='file:///workspace/dtifx-example';fs.writeFileSync(report,fs.readFileSync(report,'utf8').replaceAll(from,to));\" && dtifx diff compare ops/artifacts/diff/baseline.dtif.json tokens/catalog.json",
    "dtif:audit": "dtifx audit run --config audit/dtif-audit.config.mjs --reporter json --reporter markdown --out-dir ops/artifacts/audit",
    "verify": "npm run lint && npm run design-lint"
  },
  "dependencies": {
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@dtifx/audit": "5.0.0",
    "@dtifx/build": "5.0.0",
    "@dtifx/cli": "5.0.0",
    "@dtifx/diff": "5.0.0",
    "@eslint/js": "10.0.1",
    "@lapidist/design-lint": "7.0.1",
    "ajv": "^8.17.1",
    "eslint": "10.0.2",
    "prettier": "3.8.1"
  },
  "type": "module"
}
